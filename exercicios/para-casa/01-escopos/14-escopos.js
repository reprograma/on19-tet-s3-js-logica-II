/*
  Não altere nada no código, apenas adicione um valor a variável 'resposta'!
  Execute o código e veja se acertou no terminal
  ex: const resposta = 5;
*/
console.clear(); //Irá limpar o terminal para você
const resposta = NaN; //insira o valor da final variável 'estudo' aqui
let estudo = 10; //inicialmente declarada com let e inicializada com o valor 10.
const multiplicador = 2;

estudo = "Olá" / 500; //NaN

if (Number.isNaN(estudo)) {
  console.log("\nVocê acertou!\n");
} else {
  console.log("\nTente de novo!\n");
}

// no if a função isNaN determina se o valor é NaN ou não. 
// como estudo é um NaN, ele atende a condição que retorna true.

/*
  Você consegue justificar sua resposta?
  Comente o código como foi a manipulação dos valores
  ex:
  let estudo = 1 //aqui foi feita a atribuição no escopo global
*/

